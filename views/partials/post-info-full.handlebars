<div class="card mt-3" style="border: 0">
  <div class="card-body">
    <h3 class="card-title"><strong>{{title}}</strong></h3>
    <h6 class="card-subtitle mb-2 text-dark"><strong>by {{user.username}} on {{format_date created_at}}</strong></h6>
    <p class="card-text">{{post_body}}</p>
    <hr class="mt-3 mb-3"/>
    <h6 class="text-muted">{{comments.length}} {{format_plural "comment" comments.length}}</h6>
  </div>
</div>