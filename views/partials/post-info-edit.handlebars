<div class="card">
  <div class="card-body">
    <h5 class="card-title"><a href="/post/{{id}}">{{format_title title}}</a></h5>
    <h6 class="card-subtitle mb-2 text-dark"><strong>{{format_date created_at}}</strong></h6>
    <p class="card-text">{{format_short post_body}}</p>
    <div class="container" style="--bs-gutter-x:0">
      <div class="row">
        <div class="col">
          <h6 class="text-muted">{{comments.length}} {{format_plural "comment" comments.length}}</h6>
        </div>
        <div class="col text-end">
          <a href="/dashboard/edit/{{id}}" class="btn btn-primary">Edit post</a>
        </div>
      </div> 
    </div>  
  </div>
</div>